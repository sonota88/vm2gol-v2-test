  call main
  exit

label main
  push bp
  mov sp bp

  # 関数の処理本体
  add_sp -1
  mov 2 reg_a
  push reg_a
  mov 3 reg_a
  push reg_a
  pop reg_b
  pop reg_a
  add_ab
  mov reg_a [bp:-1]

  mov bp sp
  pop bp
  ret
