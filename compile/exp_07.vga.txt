  call main
  exit

label sub
  push bp
  mov sp bp

  # 関数の処理本体

  mov bp sp
  pop bp
  ret

label main
  push bp
  mov sp bp

  # 関数の処理本体
  _cmt call~~sub
  call sub
  add_sp 0

  mov bp sp
  pop bp
  ret
