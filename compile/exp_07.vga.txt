  call main
  exit

label sub
  push bp
  mov bp sp

  # 関数の処理本体

  mov sp bp
  pop bp
  ret

label main
  push bp
  mov bp sp

  # 関数の処理本体
  _cmt call~~sub
  call sub
  add_sp 0

  mov sp bp
  pop bp
  ret
