  call main
  exit

label f1
  push bp
  mov sp bp

  # 関数の処理本体
  mov 42 reg_a

  mov bp sp
  pop bp
  ret

  mov bp sp
  pop bp
  ret

label main
  push bp
  mov sp bp

  # 関数の処理本体
  add_sp -1
  _cmt call~~f1
  call f1
  add_sp 0
  mov reg_a [bp:-1]

  mov bp sp
  pop bp
  ret
