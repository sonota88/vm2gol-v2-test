  call main
  exit

label sub
  push bp
  mov sp bp

  # 関数の処理本体

  mov bp sp
  pop bp
  ret

label main
  push bp
  mov sp bp

  # 関数の処理本体
  mov 1 reg_a
  push reg_a
  _cmt call~~sub
  call sub
  add_sp 1

  mov bp sp
  pop bp
  ret
